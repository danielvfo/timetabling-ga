

module.exports = class MatingPool {
  constructor() {};

  var mating_pool_size = 0;

  createMatingPool() {

  };

  addToMatingPool() {

  };

  //Consider an approach for extracting pairs to breed
  extractFromMatingPool() {

  };

  setMatingPoolSize(size) {
    this.mating_pool_size = size;
  };
};
